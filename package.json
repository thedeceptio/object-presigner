{
  "name": "@thedeceptio/object-presigner",
  "version": "0.0.2",
  "description": "Generate presigned URLs and object signatures compatible with S3-like storages.",
  "keywords": [
    "s3",
    "presign",
    "object-storage",
    "url",
    "aws",
    "signature",
    "library"
  ],
  "author": "thedeceptio<thedeceptio@gmail.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/thedeceptio/object-presigner.git"
  },
  "homepage": "https://github.com/thedeceptio/object-presigner#readme",
  "bugs": {
    "url": "https://github.com/thedeceptio/object-presigner/issues"
  },
  "type": "module",
  "engines": {
    "node": ">=16"
  },
  "sideEffects": false,
  "main": "dist/index.cjs",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "files": [
    "dist/*",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "clean": "rimraf dist",
    "build": "rimraf dist && tsup src/index.js --dts --format esm,cjs --out-dir dist",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit --checkJs --allowJs",
    "test": "echo \"No tests yet\" && exit 0",
    "coverage": "c8 -r text -r lcov npm test",
    "prepare": "npm run build",
    "prepublishOnly": "npm run lint && npm run typecheck && npm run test"
  },
  "devDependencies": {
    "@eslint/js": "^9.37.0",
    "@types/node": "^16.18.126",
    "c8": "^9.0.0",
    "eslint": "^9.37.0",
    "globals": "^15.15.0",
    "rimraf": "^6.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0"
  }
}
